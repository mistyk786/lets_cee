/** @deprecated Import from @/lib/inboxMode instead. */
export {
  getInboxSessionId,
  setInboxSessionId,
  inboxSessionHeaders,
  inboxRequestHeaders,
  mapInboxSession,
  type InboxSessionInfo,
} from "./inboxMode";
